[
  {
    "__rootArray": [
      "{{__rootArray}}",
      {
        "Name": "{{Name}}",
        "Fields": [
          "{{Fields}}",
          {
            "Name": "{{Name}}",
            "ImportTemplate": "{{Name}}",
            "ImportRegex": [ "{{ToCanonical}}", "asdf" ],
            "Testo": "=>asString(Template)"
          }
        ],
        "Lines": "{{Lines}}"
      }
    ]
  },
  {
    "__rootArray": [
      "{{__rootArray}}",
      {
        "Name": "{{Name}}",
        "Fields": [
          "{{Fields}}",
          {
            "Name": "{{Name}}",
            "Testo": "=>asString(Template)"
          }
        ],
        "Lines": "{{Lines}}"
      }
    ]
  },
  {
    "__rootArray": [
      "{{__rootArray}}",
      {
        "Name": "{{Name}}"
      }
    ]
  }
]